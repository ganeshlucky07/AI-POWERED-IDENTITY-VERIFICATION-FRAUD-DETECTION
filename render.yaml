services:
  - type: web
    name: kyc-identity-verification
    runtime: docker
    plan: standard
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6
      - key: KYC_DB_PATH
        value: /data/kyc.db
